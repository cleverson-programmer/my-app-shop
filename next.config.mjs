/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['localhost','lh3.googleusercontent.com', 'imgnike-a.akamaihd.net'],
    },
  };
  
export default nextConfig;